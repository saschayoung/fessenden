from setuptools import setup

setup(name='socket_utils',
      version='0.1',
      description='A collection of simple tcp clients and servers',
      author='Alex Young',
      packages=['socket_utils'],
      zip_safe=False)
